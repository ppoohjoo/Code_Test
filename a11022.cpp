#include <iostream>
#include <stdio.h>
using namespace std;

void a11022() {



    int x,a,b;
    cin >> x;


    for (int i = 1; i <= x; i++) {
        cin >> a >> b;
        cout << "Case #" << i << ": " << a << " + " << b << " = " << a + b << '\n';
    }






}